import {CSSProperties} from 'react';

interface Props {
  className?: string;
  thumbClassName?: string;
  progress?: number;
  style?: CSSProperties;
  thumbStyle?: CSSProperties;
}

export type {Props};
